{
  "name": "buildcs",
  "version": "0.0.8",
  "description": "A build tool for mono that is controlled by a .json project file.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Raymond Nowley",
  "repository": {
    "type": "git",
    "url": "https://github.com/rnowley/buildcs.git"
  },
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^0.25.0",
    "yargs": "^3.29.0"
  },
  "devDependencies": {
    "chai": "^3.4.0"
  },
  "bin": {
    "buildcs": "bin/buildcs"
  }
}
